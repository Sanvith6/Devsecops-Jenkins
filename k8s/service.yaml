apiVersion: v1
kind: Service
metadata:
  name: starter-app
  labels:
    app: starter-app
spec:
  selector:
    app: starter-app
  ports:
  - name: http
    port: 80
    targetPort: 8080
  type: ClusterIP
